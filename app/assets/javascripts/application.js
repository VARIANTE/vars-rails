/**
 * This is a good place to attach libraries to the DOM with Browserify. Do not add code
 * directly here and only include libraries that commonly shared within the entire app.
 *
 * i.e. window.$ = require('jquery');
 */
